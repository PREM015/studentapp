import "dotenv/config"; // <-- REQUIRED so Prisma loads .env automatically
import { defineConfig } from "prisma/config";

export default defineConfig({
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
  },
});